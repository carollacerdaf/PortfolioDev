import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    header: {
        width: '100%',
        flexDirection:'row',
        marginTop: 70,
        alignItems: 'center',
        justifyContent: 'center',
    },
    title: {
        color: '#4EA8DE',
        fontSize: 24,
        marginTop:70,
    }
});