import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    rocketGo: {
        width: '100%',
        flexDirection: 'row',
        marginLeft: 9,
        marginTop: -3,
    },
    rocketShape: {
        fontSize: 8,
        color: '#5E60CE',
        fontWeight: 'bold',
    }
});